# Implementation Plans

Generated by the improve skill on 2026-07-06. Execute in the order below unless dependencies say otherwise. Each executor must read the assigned plan fully before starting, honor its STOP conditions, and update the status row when done.

## Execution Order & Status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Gate Game resource mint and burn helpers | P1 | M | - | DONE |
| 002 | Require owner for Game upgrades | P1 | S | - | DONE |
| 003 | Gate privileged state setters | P1 | M | 001 | DONE |
| 004 | Fix planet and colony resource collection identity | P1 | M | 001, 003 | TODO |
| 005 | Charge resources for colony upgrades and builds | P1 | M | 001, 004 | TODO |
| 006 | Make Game initialization one-time and timestamped | P1 | S/M | 001, 002 | DONE |
| 007 | Keep ERC721 token_of index consistent on transfers | P2 | S/M | - | DONE |
| 008 | Replace no-op requirement tests with real regressions | P2 | M | 003, 005 | TODO |
| 009 | Clean deployment docs and example credential hygiene | P2 | S | - | DONE |
| 010 | Design an offchain battle simulator contract/API seam | P3 | M | - | DONE |
| 011 | Design the multi-universe deployment lifecycle | P3 | M | 006 | TODO |
| 012 | Enforce colony limits per planet | P1 | S | 004 | TODO |
| 013 | Require transport arrival before docking | P1 | S/M | 004 | TODO |
| 014 | Harden deployment environment handling | P2 | M | 009 | DONE |
| 015 | Use checked resource arithmetic | P2 | S/M | 001, 005 | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale).

## Dependency Notes

- 003 depends on 001 because the central `Game` resource manager authorization pattern should be established before more contract-to-contract mutator gates are added.
- 004 depends on 003 because fixing collection requires explicit authorized caller paths between `Planet` and `Colony`.
- 005 depends on 004 because colony spend workflows should collect the correct home planet and colony resources before charging.
- 006 depends on 001 and 002 because registry initialization and upgrade controls are the same central trust boundary.
- 008 depends on 003 and 005 so the previously no-op requirement tests assert the fixed behavior, not the current broken behavior.
- 011 depends on 006 because a universe lifecycle design needs a clear one-time initialization model.
- 012 depends on 004 because colony generation relies on caller-to-home-planet identity, and the count fix should be verified against the corrected identity model.
- 013 depends on 004 because transport-to-colony tests depend on stable colony identity and mission bucketing semantics.
- 014 depends on 009 because script hardening should happen after tracked docs/examples stop carrying concrete private-key-looking values.
- 015 depends on 001 and 005 because checked resource arithmetic is safest after the central resource manager and spend paths have explicit authorization and charging behavior.

## Verification Commands

Use these repo-level gates after any plan that edits code:

| Purpose | Command | Expected on success |
|---------|---------|---------------------|
| Format | `scarb fmt --check` | exit 0 |
| Build | `scarb build` | exit 0 |
| Tests | `snforge test` | exit 0, all tests pass |
| Shell syntax | `bash -n scripts/deploy-starknet.sh` | exit 0 (for plan 014 only) |

## Findings Considered And Rejected

- None. All vetted findings from the audit were selected for planning by the maintainer. Findings already covered by plans 001-011 were not duplicated.
